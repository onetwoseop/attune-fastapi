# ==========================================
# [1] 현재 단계: 서버 기본 구동 및 뼈대 작업
# ==========================================
# Web Server
fastapi>=0.110.0
uvicorn[standard]>=0.29.0
python-dotenv>=1.0.0
websockets>=12.0
requests>=2.32.0
httpx>=0.27.0

# Data Validation & Utils
pydantic>=2.7.0
pydantic-settings>=2.0.0
numpy>=1.26.0

# PyTorch (GPU - CUDA 12.4 빌드, CUDA 12.6 런타임 호환)
# GTX 1660 Super / CUDA 12.6 환경용
# CUDA 버전이 다르면 cu121 / cu118 등으로 변경
torch --index-url https://download.pytorch.org/whl/cu124
torchaudio --index-url https://download.pytorch.org/whl/cu124

# --- A. LLM (Qwen2.5-1.5B-Instruct) ---
transformers>=4.45.0
accelerate>=1.0.0
protobuf>=5.0.0

# --- B. Vision (DeepFace - 얼굴 감정 인식) ---
deepface>=0.0.93
opencv-python>=4.10.0
tf-keras>=2.15.0

# --- C. Audio (Whisper STT, SpeechBrain, VAD) ---
# openai-whisper>=20231117
speechbrain>=1.0.0
soundfile>=0.12.0
faster-whisper>=1.0.0
ffmpeg-python>=0.2.0

# --- D. LoRA / 양자화 / 오디오 처리 ---
peft>=0.18.0
bitsandbytes>=0.46.1
librosa>=0.10.0