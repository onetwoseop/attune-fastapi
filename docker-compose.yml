services:
  counseling_server:
    build: .
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - whisper-cache:/root/.cache/huggingface   # 모델 캐시 유지 (Whisper, Silero VAD)
    restart: unless-stopped

volumes:
  whisper-cache:
